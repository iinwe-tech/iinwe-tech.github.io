<div class="bg-purple-h">
  <div class="mx-auto h-[80vh] flex items-center justify-center bg-purple-l">
    <div class="flex flex-col md:flex-row items-center justify-center gap-10 md:gap-28">
      <div class="bg-white rounded-full w-[200px] h-[200px] md:w-[400px] md:h-[400px] flex items-center justify-center">
        <img src="/images/illust-building.svg" alt="" class="w-[200px] md:w-[400px]" />
      </div>
      <div class="flex flex-col items-center md:items-start">
        <div class="text-3xl md:text-5xl font-bold text-gray-700 mb-10">곧 만나요!</div>
        <div class="flex flex-col gap-4">
          <button id="membershipButton" class="btn btn-primary">아이인위 멤버십 가입하기</button>
          <button id="snsButton" class="btn btn-outline-primary">아이인위 SNS 구독하기</button>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
    const membershipButton = document.getElementById('membershipButton');
    const snsButton = document.getElementById('snsButton');

    if (membershipButton) {
      membershipButton.addEventListener('click', () => {
        window.open('https://forms.gle/4GqkZByad6L2dx247');
      });
    }
    if (snsButton) {
      snsButton.addEventListener('click', () => {
        window.open('https://linktr.ee/iinwe');
      });
    }
</script>

<style lang="scss">
    .btn {
        @apply py-3 px-6 rounded-lg transition;
    }
    .btn-primary {
        @apply bg-purple-h text-white;
      &:hover {
        @apply bg-opacity-90;
      }
    }
    .btn-outline-primary {
        @apply border border-purple-h text-purple-h;
      &:hover {
        @apply bg-purple-200;
      }
    }
</style>
